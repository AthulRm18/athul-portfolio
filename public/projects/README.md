# Project media

Add your demo files here so cards and project pages show real video/GIFs instead of the animated placeholder.

| File | Project |
|------|---------|
| `cortex.mp4` | CorteX (optional `cortex-poster.jpg`) |
| `search-reranker.gif` | Search Re-Ranker |
| `surge-engine.mp4` | Surge Engine |
| `hotel-booking.gif` | Hotel Cancellations |

Paths are already wired in `src/lib/data/projects.ts`. Until files exist, each card uses a subtle CSS animation instead.
